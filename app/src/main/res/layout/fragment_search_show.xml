<?xml version="1.0" encoding="utf-8"?>
<!-- Basecamp #9960348333 (round 7 rebuild, 2026-06-04): the dedicated search
     results page is now TABBED — Shows / Products / Users — matching the PWA
     searchTabBar (resources/views/app/search.blade.php). All three tabs are
     driven by a single POST /api/v1/search call (same as the PWA), so the
     counts are consistent and we no longer depend on get-live-show's `type`
     validation (the source of several prior bounces). Default tab = Shows
     (Larry's call: livestream-shopping app, shows are the headline). A single
     "No results found" is shown only when ALL THREE sections are empty. -->
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".ui.dashboard.SearchShowFragment">

    <io.bidswipe.app.ui.custom.Header
        android:id="@+id/header"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:headerTitle="Search"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <com.google.android.material.textfield.TextInputLayout
        android:id="@+id/searchLayout"
        style="@style/inputBoxStyle"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="@dimen/margin"
        android:layout_marginEnd="4dp"
        android:layout_marginBottom="@dimen/margin"
        app:boxStrokeWidth="2dp"
        app:endIconMode="clear_text"
        app:endIconTint="@color/scrim"
        app:layout_constraintEnd_toStartOf="@id/filterBtn"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/header"
        app:startIconDrawable="@drawable/search"
        app:startIconTint="@color/outlineVariant">

        <com.google.android.material.textfield.MaterialAutoCompleteTextView
            android:id="@+id/search"
            style="@style/inputTextStyle"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="@string/what_are_you_looking_for"
            android:inputType="text" />

    </com.google.android.material.textfield.TextInputLayout>

    <com.google.android.material.button.MaterialButton
        android:id="@+id/filterBtn"
        style="@style/Widget.Material3.Button.IconButton.Filled"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginEnd="@dimen/margin"
        android:contentDescription="Filter search results"
        app:backgroundTint="@android:color/black"
        app:icon="@drawable/ic_product_filter"
        app:iconTint="@android:color/white"
        app:layout_constraintBottom_toBottomOf="@id/searchLayout"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="@id/searchLayout" />

    <!-- Tabs: Shows / Products / Users (PWA searchTabBar parity).
         Tab labels carry their result counts, set in SearchShowFragment. -->
    <com.google.android.material.tabs.TabLayout
        android:id="@+id/searchTabBar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:visibility="gone"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/searchLayout"
        app:tabGravity="fill"
        app:tabIndicatorColor="@color/scrim"
        app:tabMode="fixed"
        app:tabSelectedTextColor="@color/scrim"
        app:tabTextColor="@color/outlineVariant">

        <!-- No android:id on TabItems: they are consumed by TabLayout at inflate
             time and removed from the view tree, so ViewBinding can't bind them
             (that NPE'd: "Missing required view tabProducts"). Tabs are accessed
             by index via getTabAt() in SearchShowFragment. -->
        <com.google.android.material.tabs.TabItem
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Shows" />

        <com.google.android.material.tabs.TabItem
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Products" />

        <com.google.android.material.tabs.TabItem
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Users" />

    </com.google.android.material.tabs.TabLayout>

    <!-- Single results RecyclerView. Renders only the active tab's section
         (Shows = 2-col grid, Products/Users = full-width rows) via
         UnifiedSearchResultAdapter + its SpanSizeLookup. -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/resultsRecycler"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_margin="@dimen/half_margin"
        android:clipToPadding="false"
        android:orientation="vertical"
        app:layoutManager="androidx.recyclerview.widget.GridLayoutManager"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/searchTabBar"
        app:spanCount="2"
        tools:itemCount="4"
        tools:listitem="@layout/home_item" />

    <!-- Single empty state — shown only when query is non-blank AND all three
         sections (shows + products + users) are empty. -->
    <io.bidswipe.app.ui.custom.NoDataView
        android:id="@+id/noData"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:visibility="gone"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/searchTabBar"
        app:title_text="No results found" />

    <io.bidswipe.app.ui.custom.LoaderView
        android:id="@+id/loader"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:visibility="gone" />

</androidx.constraintlayout.widget.ConstraintLayout>
