<?xml version="1.0" encoding="utf-8"?>
<!-- Basecamp #9940152629 (2026-05-29): tip settings for a scheduled show.
     Fields: tip_message + show_in_live_chat, keyed by schedule_show_id.
     Mirrors seller_tip_settings_sheet.xml (in-live version) as a full Activity. -->
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    tools:context=".ui.scheduleShow.TipSettingActivity">

    <io.bidswipe.app.ui.custom.Header
        android:id="@+id/header"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:headerTitle="Tip Settings"
        app:layout_constraintTop_toTopOf="parent"
        app:showBackButton="true" />

    <androidx.core.widget.NestedScrollView
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:fillViewport="true"
        app:layout_constraintTop_toBottomOf="@+id/header"
        app:layout_constraintBottom_toTopOf="@+id/btnSubmit">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="@dimen/margin">

            <!-- Description -->
            <TextView
                android:id="@+id/tvSubtitle"
                style="@style/BodyMedium"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginBottom="@dimen/margin"
                android:drawableStart="@drawable/ic_shine"
                android:drawablePadding="4dp"
                android:text="Showcase your personality and boost your chances of receiving tips. This message will be displayed in your live store."
                android:textColor="@color/onSurfaceVariant" />

            <!-- Tip message input -->
            <TextView
                style="@style/TitleMedium"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginBottom="@dimen/half_margin"
                android:text="Tip Message"
                android:textColor="@color/onSurface" />

            <com.google.android.material.textfield.TextInputLayout
                style="@style/inputBoxStyle"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                app:counterEnabled="true"
                app:counterMaxLength="140">

                <com.google.android.material.textfield.TextInputEditText
                    android:id="@+id/etTipMessage"
                    style="@style/inputTextStyle"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:hint="Try something like: &quot;Tip for a shoutout!&quot;"
                    android:inputType="textMultiLine|textCapSentences"
                    android:maxLines="4" />

            </com.google.android.material.textfield.TextInputLayout>

            <!-- Divider -->
            <View
                android:layout_width="match_parent"
                android:layout_height="1dp"
                android:layout_marginVertical="@dimen/margin"
                android:background="@color/surfaceVariant" />

            <!-- Show in live chat toggle -->
            <TextView
                style="@style/TitleMedium"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginBottom="@dimen/half_margin"
                android:text="Show buyer tip messages in live chat"
                android:textColor="@color/onSurface" />

            <com.google.android.material.materialswitch.MaterialSwitch
                android:id="@+id/switchShowInLiveChat"
                style="@style/appSwitch"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Turn this on to display buyer tip messages in the live chat for all viewers."
                android:textAppearance="@style/BodySmall"
                android:textColor="@color/onSurfaceVariant"
                android:checked="true" />

        </LinearLayout>

    </androidx.core.widget.NestedScrollView>

    <!-- Save button fixed at bottom -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/btnSubmit"
        style="@style/appBtn"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="@dimen/margin"
        android:minHeight="56dp"
        android:text="Save Tip Settings"
        app:layout_constraintBottom_toBottomOf="parent" />

    <io.bidswipe.app.ui.custom.LoaderView
        android:id="@+id/loader"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:visibility="gone" />

</androidx.constraintlayout.widget.ConstraintLayout>
